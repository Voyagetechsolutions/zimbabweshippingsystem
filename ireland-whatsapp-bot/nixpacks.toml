# Nixpacks build config for Railway.
# Pins Node 20 (Baileys + Supabase work cleanly here).
[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci --omit=dev || npm install --omit=dev']

[start]
cmd = 'node bot.js'
